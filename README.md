# Drag-and-drop
🔧 To “run” the code:

Open the folder in your editor (you already are in Drag-and-drop).

Launch the page in a browser
Double‑click index.html in Explorer, or
Right‑click it in VS Code’s Explorer and choose “Open with Default Browser” (or install the “Live Server” extension and hit Go Live).
# from the workspace root, using Python 3:
python -m http.server 8000

# then open http://localhost:8000 in your browser!!

That’s all—once the HTML file is opened, your script.js and style.css will load automatically.
READ TO RUN !!!